<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>

    <style>
        /* General Styles */
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(to right, #887570, #d87047);
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-image: url('girl.jpg'); /* Update with your image path */
            background-repeat: no-repeat;
            background-size: cover;
        }
        
        .container {
            background: transparent;
            padding: 40px;
            border-radius: 10px;
            background-color: rgba(41, 36, 36, 0.5);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
            width: 1250px;
            text-align: center;
            transition: transform 0.2s ease-in-out;

        }
        
        .container:hover {
            transform: translateY(-10px);
        }
        
        /* Form Elements */
        input[type="text"], 
        input[type="email"], 
        input[type="password"] {
            width: 100%;
            padding: 15px;
            margin: 10px 0;
            box-sizing: border-box;
            border: 1px solid #ddd;
            border-radius: 5px;
            transition: border-color 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
        }
        
        input[type="text"]:focus,
        input[type="email"]:focus,
        input[type="password"]:focus {
            border-color: #ff7e5f;
            box-shadow: 0 0 5px rgba(255, 126, 95, 0.5);
            outline: none;
        }
        p{
            color: white;
            
        }
        
        button {
            padding: 15px 25px;
            background-color: #ad8176;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            transition: background-color 0.2s ease-in-out, transform 0.2s ease-in-out;
        }
        
        button:hover {
            background-color: #feb47b;
            transform: translateY(-2px);
        }
        
        button:active {
            background-color: #ff7e5f;
            transform: translateY(0);
        }
        
        /* Links */
        a {
            color: #f5f3f3;
            text-decoration: none;
            transition: color 0.2s ease-in-out;
        }
        
        a:hover {
            text-decoration: underline;
            color: #feb47b;
        }
        
        /* Headings */
        h1 {
            font-size: 2em;
            color: #ece9e9;
            margin-bottom: 20px;
            font-weight: 700;
            text-transform: uppercase;
        }
        
        /* Additional Styles */
        .container .form-description {
            font-size: 0.9em;
            color: #666;
            margin-bottom: 20px;
        }
        
        .container .form-error {
            color: #ff0000;
            margin-bottom: 20px;
            font-size: 0.9em;
        }
        
        .container .form-success {
            color: #28a745;
            margin-bottom: 20px;
            font-size: 0.9em;
        }
        
        h2 {
            text-align: center;
            padding-bottom: 500px;
        }
        h4{
            text-align: center;
            padding-bottom: 800px;
        }
            /* Headings */
        h1,h2,h3,h4 {
            font-size: 2em;
            color: #fcf6f7;
            margin-bottom: 20px;
            font-weight: 700;
            text-transform: uppercase;
            transition: color 0.5s ease-in-out, transform 0.3s ease-in-out;
        }


        h1:hover {
            color: #ff7e5f; 
            transform: scale(1.1); 
        }

        
    </style>
</head>
<body>
    <h2>New users should sign up first!....</h2>
    <marquee behavior="" direction=""><h4>Welcome to the login page!........</h4></marquee>
    <div class="container">
        <h1>Login</h1>
        <form id="login-form">
            <input id="email" type="text" placeholder="Email" required><br><br>
            <input id="password" type="password" placeholder="Password" required><br><br>
            <input type="checkbox" name="" id="" required><p>i agree to the terms and conditions</p><br>
            <button type="submit">Login</button><br><br>
            <a href="/signup">Sign Up</a>
        </form>
    </div>
    <marquee behavior="" direction=""><h3>login with valid credentials</h3></marquee>

    <script>
        document.getElementById('login-form').addEventListener('submit', async (e) => {
            e.preventDefault();
            const email = document.getElementById('email').value;
            const password = document.getElementById('password').value;

            try {
                const response = await fetch('/login', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({ email, password })
                });

                if (response.ok) {
                    window.location.href = '/landing-page';
                } else {
                    const errorText = await response.text();
                    alert(`Error: ${errorText}`);
                }
            } catch (error) {
                console.error('Error occurred:', error);
                alert('An error occurred. Please try again later.');
            }
        });
    </script>  
</body>
</html>
